# 技能:网络关系图(语义)
- type=network。每个节点 = 一个实体(顶层,parent 为 null);用 edges 表达实体之间的关系,edge.label 写关系名(如 "依赖"、"归属"、"调用")。
- 适合关系网、依赖图、组织/系统的多对多连接;不是树,允许任意节点互连。
- 用 detail 承载实体的简短说明。emphasis=primary 可高亮核心实体。
- 语言随环境。不输出坐标、颜色、尺寸。
