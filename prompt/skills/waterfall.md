# 技能:瀑布图(语义)
- type=waterfall。每个顶层节点 = 一步变化,按顺序排列。节点的 score.x = 该步的增减量(可为负);score.y 也填该值(校验需要 x、y 都在)。
- 第一步通常是起始量(正值),中间步为增减,浮动条自动累计。label = 步骤名(如 "期初"、"新增收入"、"成本")。
- 适合财务拆解、指标归因、增长/流失分解。
- 语言随环境。不输出坐标、颜色、尺寸。
