# 技能:看板图(语义)
- type=kanban。先给 diagram.lanes(列的顺序,如 ["待办","进行中","已完成"]);每个顶层节点 = 一张卡片,必须带 lane(在 lanes 内);同一列卡片按输入顺序纵向堆叠。
- 用 detail 承载卡片的补充说明(负责人、要点),保持简短。列名即状态/阶段,不要把状态写进卡片标签。
- 一般不需要 edges;看板表达分栏归类而非流转箭头。
- 语言随环境。不输出坐标、颜色、尺寸。
