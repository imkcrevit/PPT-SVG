# 技能:雷达图(语义)
- type=radar。每个顶层节点 = 一个维度(轴),建议 3-8 个。节点的 score.x = 该维度的值(0..1,越大越靠外);score.y 同样填该值(校验需要 x、y 都在)。
- label = 维度名(如 "性能"、"成本"、"易用性")。detail 可省略。
- 适合能力模型、竞品多维对比、评估打分。
- 语言随环境。不输出坐标、颜色、尺寸。
